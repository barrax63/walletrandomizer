# Example .env file for Docker Compose
# Copy this file to .env and configure your settings

# =============================================================================
# BALANCE API CONFIGURATION
# =============================================================================
# Balance API to use: "fulcrum" or "blockcypher"
# - fulcrum: Use local Fulcrum Electrum server (faster, requires setup)
# - blockcypher: Use Blockcypher public API (slower, no setup required)
# Default: blockcypher
BALANCE_API=blockcypher

# =============================================================================
# FULCRUM SERVER CONFIGURATION (if BALANCE_API=fulcrum)
# =============================================================================
# Fulcrum server address in format host:port
# Example: FULCRUM_SERVER=192.168.1.100:50001
FULCRUM_SERVER=127.0.0.1:50001

# Alternative: Separate host and port
FULCRUM_HOST=127.0.0.1
FULCRUM_PORT=50001

# =============================================================================
# BLOCKCYPHER API CONFIGURATION (if BALANCE_API=blockcypher)
# =============================================================================
# Blockcypher API base URL
# Default: https://api.blockcypher.com/v1/btc/main
BLOCKCYPHER_API_URL=https://api.blockcypher.com/v1/btc/main

# Blockcypher API Token (optional, but recommended)
# Provides higher rate limits for authenticated requests
# Without a token, the unauthenticated API has rate limits (~200 requests/hour)
# To obtain an API token:
# 1. Visit https://accounts.blockcypher.com/
# 2. Sign up for a free account
# 3. Create an API token
# 4. Add your API token here
# Example: BLOCKCYPHER_API_TOKEN=your-api-token-here
# BLOCKCYPHER_API_TOKEN=

# Rate limit delay between API requests (in seconds)
# The Blockcypher API has rate limits (~200/hour without token, ~2000/hour with token)
# Default: 0.5 seconds
# The app also uses exponential backoff on 429 responses
BLOCKCYPHER_RATE_LIMIT=0.5

# =============================================================================
# WALLET GENERATION CONFIGURATION
# =============================================================================
# Number of wallets to generate
# Use -1 for infinite wallet generation (continuous mode)
# Default: -1 (infinite)
NUM_WALLETS=-1

# Number of addresses to derive per wallet
# Default: 10
NUM_ADDRESSES=10

# Comma-separated BIP derivation types
# Options: bip44, bip49, bip84, bip86
# Default: bip84
NETWORK=bip84

# Mnemonic word count (12 or 24)
# Default: 12
WORD_COUNT=12

# Mnemonic language
# Options: english, french, italian, spanish, korean, chinese_simplified, chinese_traditional
# Default: english
LANGUAGE=english

# =============================================================================
# OUTPUT CONFIGURATION
# =============================================================================
# Output directory for wallet JSON files (inside container)
# Default: /data
OUTPUT_PATH=/data

# =============================================================================
# WEB SERVER CONFIGURATION
# =============================================================================
# Web server host (usually 0.0.0.0 to accept connections from any interface)
WEB_HOST=0.0.0.0

# Web server port
WEB_PORT=5000

# Number of uvicorn worker processes
# IMPORTANT: Keep this at 1 because generation state is stored in-memory per-process.
# Multiple workers would each have their own state, causing the web UI to show
# inconsistent data (e.g., stuck on "Initializing...").
# If you need horizontal scaling, use an external state store like Redis.
WEB_WORKERS=1

# Request timeout in seconds
WEB_TIMEOUT=120

# Port where nginx exposes the service to
NGINX_PORT=4099
